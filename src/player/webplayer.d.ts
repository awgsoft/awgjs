import { Sequence } from "../sequence/sequence";

declare class WebPlayer {
  constructor(sequence: Sequence);
  // mute(trackNum: number, muted: boolean): void
  // isMuted(trackNum: number): boolean
}
